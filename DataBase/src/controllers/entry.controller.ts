// Controlador para el ingreso de los aprendices

import { Request, Response } from 'express'
import { query } from '../config/db

// funcion para añadir una entrada al CTA
export const addEntry = async  (Request : Request, Response : Response) => {
  try{

  } catch (error) {

  }
}
